{
  "devDependencies": {
    "sass": "^1.51.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-to-css": "sass -w styles.scss styles.css",
    "devStart": "npx browser-sync start --server --directory --logLevel 'debug'"
  }
}
